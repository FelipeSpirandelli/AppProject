import {  StyleSheet  } from 'react-native';

const textStyle = StyleSheet.create({
    primaryText: {
        marginBottom: 4,
        fontSize: 20,
        color: '#000',
    },
})

export default textStyle;
